#
# Modules for running ansible and modules used in many playbooks
# Modules used in specific playbooks are installed at the point of use.
#
ansible == 2.9.14
boto == 2.49.0
boto3 == 1.15.18
jmespath == 0.10.0
