---

# Provide details of the compute cluster nodes.
# It consists of a head node and database severs.
all:
  children:
    head:
      hosts:
        localhost:
    dbservers:
      fragger_production_database:
        192.168.253.65:
