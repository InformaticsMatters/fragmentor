---

# The DB server's volume devices, and mount-points
db_devices:
  pg_data:
    device: /dev/vdb
    mount_path: /pgdata
  pg_copy:
    device: /dev/vdc
    mount_path: /pgcopy
  pg_wal:
    device: /dev/vdd
    mount_path: /pgwal
