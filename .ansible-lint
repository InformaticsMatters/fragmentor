---

# Lint Ansible but exclude some forms of error.
# See https://docs.ansible.com/ansible-lint/rules/default_rules.html
#
# 204 : Lines should be no longer than 160 chars
# 301 : Commands should not change things if nothing needs doing
# 306 : Shells that use pipes should set the pipefail option (pipefail not working on ubuntu)
# 302 : rm used in place of argument state=absent to file module
# 303 : git used in place of git module
# 305 : Use shell only when shell functionality is required
skip_list:
- '204'
- '301'
- '306'
- '302'
- '303'
- '305'