process {
    executor = 'slurm'
    container = 'busybox:latest'
}
singularity {
    enabled = true
    cacheDir = "$HOME/singularity-containers"
    autoMounts = true
}
executor {
    queueSize = 280
}

